const config = {
    client: "mysql",
    connection: {
        host: "localhost",
        user: "developer",
        password: "Bianca1$",
        database: "finance"
    }
};

module.exports = config;